package com.example.ladybugos.claude_note

import kotlinx.coroutines.flow.Flow

/*
class GetAllNotesUseCase(private val repository: KNoteRepository) {
    operator fun invoke(): Flow<List<KNote>> = repository.getAllNotes()
}

class GetNoteByIdUseCase(private val repository: KNoteRepository) {
    operator fun invoke(id: Long): Flow<KNote?> = repository.getNoteById(id)
}

class GetNotesByTagUseCase(private val repository: KNoteRepository) {
    operator fun invoke(tagId: Long): Flow<List<KNote>> = repository.getNotesByTagId(tagId)
}

class GetAllTagsUseCase(private val repository: KNoteRepository) {
    operator fun invoke(): Flow<List<KTag>> = repository.getAllTags()
}

class AddNoteUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(note: KNote) = repository.insertNote(note)
}

class UpdateNoteUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(note: KNote) = repository.updateNote(note)
}

class DeleteNoteUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(note: KNote) = repository.deleteNote(note)
}

class AddTagUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(tag: KTag) = repository.insertTag(tag)
}

class UpdateTagUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(tag: KTag) = repository.updateTag(tag)
}

class DeleteTagUseCase(private val repository: KNoteRepository) {
    suspend operator fun invoke(tag: KTag) = repository.deleteTag(tag)
}*/
